name: Manual Workflow with Checkboxes
on:
  workflow_dispatch:
    inputs:
      checkbox_1:
        description: 'Checkbox 1 description'
        required: false
        default: false
      checkbox_2:
        description: 'Checkbox 2 description'
        required: false
        default: false
      checkbox_3:
        description: 'Checkbox 3 description'
        required: false
        default: false
      checkbox_4:
        description: 'Checkbox 4 description'
        required: false
        default: false

jobs:
  run_workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Display selected checkboxes
        run: |
          echo "Checkbox 1: ${{ github.event.inputs.checkbox_1 }}"
          echo "Checkbox 2: ${{ github.event.inputs.checkbox_2 }}"
          echo "Checkbox 3: ${{ github.event.inputs.checkbox_3 }}"
          echo "Checkbox 4: ${{ github.event.inputs.checkbox_4 }}"
